# Static Review: sample-infra

## Summary

- Files scanned: 4
- Findings: 15
- Terraform findings: 5
- Kubernetes findings: 10

## Findings

- [low] kubernetes `kubernetes/deployment.yaml:1` K8S_DEFAULT_NAMESPACE - Workload manifest does not set metadata.namespace in scanned files.
  Recommendation: Confirm namespace is set by deployment tooling or add it explicitly.
- [high] kubernetes `kubernetes/deployment.yaml:1` K8S_HOST_NETWORK - Workload uses hostNetwork in scanned files.
  Recommendation: Confirm host networking is required and document network isolation controls.
- [high] kubernetes `kubernetes/deployment.yaml:1` K8S_HOST_PATH_VOLUME - Workload mounts a hostPath volume in scanned files.
  Recommendation: Prefer non-hostPath storage or document the node filesystem access requirement.
- [medium] kubernetes `kubernetes/deployment.yaml:1` K8S_LATEST_IMAGE_TAG - Container `web` uses a mutable or missing image tag in scanned files.
  Recommendation: Pin images to an immutable digest or explicit non-latest tag.
- [low] kubernetes `kubernetes/deployment.yaml:1` K8S_MISSING_LIVENESS_PROBE - Container `web` is missing a liveness probe in scanned files.
  Recommendation: Add a liveness probe when restart-based recovery is appropriate.
- [medium] kubernetes `kubernetes/deployment.yaml:1` K8S_MISSING_READINESS_PROBE - Container `web` is missing a readiness probe in scanned files.
  Recommendation: Add a readiness probe so traffic is sent only after the workload is ready.
- [medium] kubernetes `kubernetes/deployment.yaml:1` K8S_MISSING_RESOURCE_LIMITS - Container `web` is missing CPU or memory limits in scanned files.
  Recommendation: Set CPU and memory limits or document why limits are intentionally omitted.
- [medium] kubernetes `kubernetes/deployment.yaml:1` K8S_MISSING_RESOURCE_REQUESTS - Container `web` is missing CPU or memory requests in scanned files.
  Recommendation: Set CPU and memory requests based on expected steady-state usage.
- [high] kubernetes `kubernetes/deployment.yaml:1` K8S_PRIVILEGED_CONTAINER - Container `web` runs privileged in scanned files.
  Recommendation: Avoid privileged containers or document the isolation exception and compensating controls.
- [low] kubernetes `kubernetes/service.yaml:1` K8S_SERVICE_SELECTOR_WITHOUT_MATCHING_WORKLOAD - No matching workload labels were found in scanned files for this Service selector.
  Recommendation: Confirm the matching workload is included in the review input or generated by deployment tooling.
- [high] terraform `terraform/iam.tf:8` TF_WILDCARD_IAM_ACTION - Wildcard IAM action was found in Terraform text.
  Recommendation: Scope IAM actions to the smallest required action set.
- [high] terraform `terraform/iam.tf:9` TF_WILDCARD_IAM_RESOURCE - Wildcard IAM resource was found in Terraform text.
  Recommendation: Scope IAM resources to specific ARNs or document the exception.
- [medium] terraform `terraform/iam.tf:15` TF_SENSITIVE_OUTPUT - Sensitive-looking output `api_token` is not marked sensitive.
  Recommendation: Add `sensitive = true` or confirm the output cannot expose secret material.
- [low] terraform `terraform/security_group.tf:1` TF_MISSING_TAGS - No tags were found in this `aws_security_group` resource block.
  Recommendation: Confirm ownership, service, environment, and cost tags are provided here or through provider defaults.
- [high] terraform `terraform/security_group.tf:9` TF_PUBLIC_INGRESS - Public ingress CIDR was found in Terraform text.
  Recommendation: Confirm this is intentionally public and restrict CIDR ranges where possible.

## Assumptions

- Static review does not execute commands, connect to external services, or inspect live infrastructure.
- Terraform review does not evaluate modules, variables, provider schemas, or plan output.
- Kubernetes review does not contact an API server or validate manifests against a live cluster.

## Verification Questions

- Commands are documentation only; do not execute them from this generated report.
- Run `terraform validate` or `tofu validate` in the relevant module.
- Review `terraform plan` for replacements, public exposure, and IAM blast radius.
- Validate Kubernetes manifests with the repository's normal manifest tooling before deploy.
